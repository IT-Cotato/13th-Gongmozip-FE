# 채팅방 구현 진도

> 작업 메모용 파일입니다. 절대 스테이징/커밋하지 않습니다.

## 현재 Git 상태

- 현재 브랜치: `feature/chat`
- 현재 HEAD: `9ed0300` (`develop`을 `feature/chat`에 머지한 로컬 병합 커밋)
- `origin/develop..HEAD` 기준 실제 파일 diff는 없음
  - 로컬 `feature/chat`은 `develop`보다 내용상 추가 변경이 있는 상태는 아님
  - 로컬에 남은 앞선 커밋은 병합 커밋 성격
- `origin/feature/chat` 기준으로는 로컬이 19커밋 앞서 있음
  - 원격 `feature/chat`이 오래된 상태라 그렇게 보임
  - 이미 `feature/chat` PR #17은 `develop`에 병합되어 있음
- 현재 미커밋 파일
  - `src/app/chat/[roomId]/menu/page.tsx`: 채팅방 메뉴 세부 동작 보강 중
  - `src/app/chat/_components/icons.tsx`: 실제 코드 diff 없음, 줄바꿈(LF/CRLF) 경고로 modified 표시
  - `채팅방.md`: 작업 메모 파일, 커밋 대상 아님

## 완료된 작업

- `/chat` 채팅방 목록 화면 구현
  - 파일: `src/app/chat/page.tsx`
  - 실제 화면 구성: `src/app/chat/_components/ChatListShell.tsx`
  - Figma `5. 1 채팅목록` 기준으로 라우트 구성
  - 채팅방 row 높이, padding, avatar stack, 채팅방 이름, 시간, 미리보기 텍스트 스타일 조정
  - 채팅방 클릭 시 `/chat/[roomId]`로 이동
- `/chat/[roomId]` 채팅방 화면 구현
  - 파일: `src/app/chat/[roomId]/page.tsx`
  - 상단 채팅방 헤더, 매칭 완료 날짜 라벨, 메시지 리스트, 입력바 구성
  - `ChatTopBar`, `ChatMessageBubble`, `ChatInputBar` 컴포넌트 분리
  - 실제 PWA 대응용 safe-area padding 적용
- 채팅방 메뉴 화면 구현
  - 파일: `src/app/chat/[roomId]/menu/page.tsx`
  - 채팅방 상단 메뉴 버튼에서 `/chat/[roomId]/menu`로 이동
  - 대화상대 목록 표시
  - 챗봇 추가/삭제 토글 UI 구현
  - 채팅방 나가기 버튼 및 확인 팝업 구현
  - 팀원 프로필 바텀시트 구현
  - 팀원 신고 다이얼로그와 신고 사유 드롭다운 구현
  - 신고 완료 피드백, 중복 신고 방지, 기타 사유 직접 입력 구현
  - Figma 직접 입력 상태 기준으로 신고 사유 입력칸 44px input 및 focus 활성 테두리 적용
- 채팅방 목록 관리/정렬 UI 구현
  - 설정 드롭다운에서 채팅방 관리 진입
  - 최신 메시지 순 / 안읽은 메시지 순 정렬 옵션 구현
  - 채팅방 선택 상태 구현
  - 선택 후 읽음 / 나가기 액션 버튼 상태 구현
- `BottomNavigation` 공통 컴포넌트 정리
  - 파일: `src/components/layout/BottomNavigation.tsx`
  - 홈, 공모전, 팀매칭, 채팅방, 마이페이지 탭 구성
  - 현재 경로 기반 active 상태 처리
  - 채팅방 unread badge 표시
  - `/chat`, `/contests`, `/team-matching`, `/mypage`에서 사용 확인
- 채팅 mock 데이터 분리
  - 파일: `src/app/chat/_data/mockMessages.ts`
  - `MOCK_CHAT_ROOMS`
  - `MOCK_CHAT_MESSAGES`
  - `MOCK_CHAT_MEMBERS`
  - `MOCK_CHATBOT_MEMBER`
  - `avatarSrc`, `avatarSrcs`로 SVG 에셋 적용 가능
- 채팅 SVG 에셋 적용
  - `public/icons/chat/tabler_settings.svg`
  - `public/icons/chat/character1.svg`
  - `public/icons/chat/character2.svg`
  - `public/icons/chat/character3.svg`
  - `public/icons/chat/chat_bot.svg`
- 전역 기본 폰트 stack 정리
  - `Pretendard`, `Roboto`, 한국어 시스템 폰트 fallback
  - `<html lang="ko">` 적용

## 추가 확인 필요

- 일부 파일을 터미널에서 읽을 때 한글 문자열이 깨져 보이는 구간이 있음
  - 확인된 파일: `src/components/layout/BottomNavigation.tsx`, `src/app/chat/[roomId]/page.tsx`, `src/app/chat/[roomId]/menu/page.tsx`, `src/app/chat/_components/ChatTopBar.tsx`
  - 실제 브라우저 표시와 빌드 결과 기준으로 정상 여부 확인 필요
- `src/app/chat/_components/icons.tsx`는 줄바꿈 차이만 있는지 정리 필요
- 마지막 검증 이후 변경이 있었다면 PR 전 `pnpm lint`, `pnpm build` 재실행 필요

## 이전 검증 기록

- `eslint src/app/chat` 통과
- `eslint src/app/chat src/app/layout.tsx` 통과
- `tsc --noEmit` 통과
- `next build` 통과

## 최근 검증 기록

- `node_modules/.bin/eslint.cmd src/app/chat/[roomId]/menu/page.tsx` 통과
- `node_modules/.bin/tsc.cmd --noEmit` 통과
- `pnpm` / `pnpm.cmd exec`는 PowerShell 실행 정책 및 pnpm 의존성 상태 확인 이슈로 직접 실행하지 못함

## 다음 작업

1. 한글 인코딩/문자열 표시 확인
   - 브라우저에서 하단 네비게이션 라벨, 채팅방 헤더, 메뉴, 신고 다이얼로그 문구가 정상 표시되는지 확인
   - 깨져 보이면 해당 파일 문자열을 UTF-8 기준으로 복구
2. 채팅방 설정 / 메시지 관리 화면 구현
   - Figma `5.2 채팅방 설정`, `5.2.1 채팅방 설정`
   - 메시지 선택 상태
   - 정렬/관리 드롭다운
3. 팀원 관련 세부 플로우 보강
   - 팀원 이름 수정
   - 팀원 프로필 정보 실제 데이터 연동 대비
4. 채팅 이벤트성 UI 구현
   - 자기소개 유도
   - 팀장 추천/투표
   - 중간점검
   - 팀원 리뷰
5. 실제 연동 준비
   - API/WebSocket 계약 확인
   - mock 데이터를 query/socket 기반 데이터로 교체
   - 로딩/빈 상태/에러 상태 추가
# 최신 작업 현황

## 완료된 작업

- 채팅방 메뉴 UX 개선
  - 파일: `src/app/chat/[roomId]/menu/page.tsx`
  - 신고 사유 선택, 기타 사유 직접 입력, 신고 완료 피드백을 구현했다.
  - 신고 완료된 팀원은 중복 신고되지 않도록 버튼 상태를 비활성화했다.
  - 채팅방 나가기 확인 팝업을 추가했다.
- 채팅 입력바 개선
  - 파일: `src/app/chat/_components/ChatInputBar.tsx`
  - 한 줄 input을 textarea로 변경했다.
  - 입력 내용에 따라 높이가 자동으로 늘어나도록 처리했다.
- 팀장 선출 mock 플로우 구현
  - 파일: `src/app/chat/_components/LeaderElectionFlow.tsx`
  - 연결 라우트: `src/app/chat/[roomId]/page.tsx`, `src/app/chat/[roomId]/leader-election/page.tsx`
  - Figma `2110:44916` 기준으로 `팀장 여부 투표` 바텀시트를 구현했다.
  - 팀장 여부 제출 후 기존 `팀장 여부 투표하기` 버튼이 비활성화되도록 처리했다.
  - Figma `2110:44628` 기준으로 챗봇의 `팀장 투표하기` 후속 메시지를 구현했다.
  - Figma `2110:44922`, `2110:44954` 기준으로 후보 2명/3명 팀장 투표 바텀시트를 구현했다.
  - Figma `2110:44957` 기준으로 `투표 완료` 바텀시트를 구현했다.
  - Figma `2110:44283` 기준으로 팀장 확정 결과 알림 챗봇 메시지와 팀장 프로필 카드를 구현했다.
  - Figma `2110:44551` 기준으로 동률 발생 메시지와 `추천수락 / 재투표하기` 버튼을 구현했다.
  - Figma `2110:44617` 기준으로 재투표 안내 메시지와 재투표 진입 버튼을 구현했다.
  - `추천수락`을 누르면 추천 후보를 팀장으로 확정한 결과 알림으로 이어지게 했다.
  - `재투표하기`를 누르면 재투표 안내 메시지를 띄우고, 다시 후보 투표 바텀시트로 진입하게 했다.
  - 실제 채팅방 `/chat/[roomId]`가 팀장 선출 플로우 컴포넌트를 렌더링하도록 연결했다.
- 채팅 팀장 선출 에셋 추가
  - 파일: `public/icons/chat/medal.svg`
  - 팀장 투표/결과 UI의 메달 이미지를 SVG 에셋으로 교체했다.
- 커밋 완료
  - `59fcc0b feat: 채팅방 메뉴와 입력 UX 개선`
  - `4be8636 feat: 채팅 팀장 선출 플로우 구현`

## 이후 진행 작업

- 팀장 선출 플로우 추가 커밋
  - 현재 팀장 선출 결과 알림, 동률, 재투표 분기와 실제 채팅방 라우트 연결 변경이 남아 있다.
  - 검증 후 별도 커밋으로 남긴다.
- 백엔드 이벤트/API 연동
  - 팀장 여부 투표 완료 여부를 서버 응답으로 판단한다.
  - 팀장 후보 풀을 서버 응답으로 받는다.
  - 팀장 후보 투표 완료 여부를 서버 응답으로 판단한다.
  - 최다 득표자, 동률 여부, 추천 팀장 정보를 서버 응답으로 받는다.
  - `추천수락`, `재투표하기` 액션을 API 또는 WebSocket 이벤트로 연결한다.
- 상태 관리 정리
  - 현재 mock 상태는 컴포넌트 내부 `useState`로 관리한다.
  - 실제 연동 시 TanStack Query cache 또는 WebSocket 수신 이벤트 기반 상태로 교체한다.
- UI 검증
  - 모바일 390px 기준으로 채팅 메시지, 바텀시트, 후보 카드 배치가 Figma와 맞는지 확인한다.
  - 후보 2명/3명, 정상 선출, 동률, 재투표 케이스를 각각 확인한다.
- 문구/인코딩 정리
  - 일부 기존 채팅 관련 파일과 이 문서의 이전 구간에 깨진 한글이 남아 있다.
  - 브라우저 표시 기준으로 깨지는 문구가 있으면 UTF-8 한국어 문구로 정리한다.
