# 채팅방 API 연동 정리

채팅방은 팀 단위로 생성되며, 화면 진입 시 필요한 기본 데이터는 REST API로 조회하고 이후 새 메시지는 STOMP WebSocket으로 반영한다.

팀장 선출, 공모전 추천, 공모전 후보/투표처럼 단계가 있는 기능도 별도 화면을 폴링하지 않고 대부분 채팅 카드 메시지로 진행한다. 프론트는 메시지의 `messageType`과 `metadata`를 기준으로 카드 UI를 렌더링하고, 사용자의 선택이 필요한 일부 액션만 REST API로 호출한다.

## 1. REST API

### 채팅

| Method | Path | 설명 |
| --- | --- | --- |
| GET | `/api/teams` | 채팅방(팀) 목록 조회 |
| GET | `/api/teams/{teamId}/members` | 대화상대(팀원) 목록 조회 |
| DELETE | `/api/teams/{teamId}/members/me` | 채팅방 나가기. 협업거리 -10m |
| PATCH | `/api/teams/{teamId}/chatbot` | 챗봇 추가/삭제. body `{ "enabled": boolean }` |
| GET | `/api/teams/{teamId}/messages` | 메시지 목록 조회. 최신순, 최근 50개 |
| PATCH | `/api/teams/{teamId}/read` | 채팅방 읽음 처리 |
| POST | `/api/reports` | 사용자 신고. body `{ reportedMemberId, teamId?, reasonCode, customReasonText? }` |

### 팀장 선출

팀장 선출은 채팅방 안의 카드 메시지로 진행된다. 프론트는 아래 액션 API만 호출하고, 현재 어떤 UI를 보여줄지는 채팅 메시지의 `messageType`과 `metadata`로 판단한다.

| Method | Path | 설명 |
| --- | --- | --- |
| PATCH | `/api/teams/{teamId}/leader-candidacy` | 팀장 여부 투표. body `{ "wants": boolean }` |
| POST | `/api/teams/{teamId}/leader-votes` | 팀장 투표. body `{ "candidateTeamMemberId": number }` |
| POST | `/api/teams/{teamId}/leader-votes/ai-recommendation/accept` | 동률 시 AI 추천 수락. body 없음 |
| POST | `/api/teams/{teamId}/leader-votes/revote` | 동률 시 재투표 시작 안내. body 없음 |

## 2. WebSocket(STOMP)

| 방향 | Destination | 설명 |
| --- | --- | --- |
| 클라이언트 -> 서버 | `/app/teams/{teamId}/messages` | 메시지 전송. body `{ "content": string }` |
| 서버 -> 클라이언트 | `/topic/teams/{teamId}` | 팀 채팅방 실시간 브로드캐스트 구독. 멤버, 챗봇, 시스템 메시지 모두 이 토픽으로 수신 |
| 서버 -> 클라이언트 | `/user/queue/errors` | 본인에게만 오는 처리 실패 에러 메시지 |

채팅방에 들어오면 먼저 `GET /api/teams/{teamId}/messages`로 최근 메시지를 채운 뒤 WebSocket을 구독한다. 서버가 WebSocket 구독 전에 이미 발행한 메시지(예: 팀 생성 직후 챗봇 인사)는 실시간 브로드캐스트로 받을 수 없기 때문이다.

## 3. 메시지 렌더링 기준

메시지의 `messageType`은 일반 메시지, 시스템 메시지, 카드형 메시지 종류를 구분하는 값이다. 카드 렌더링에 필요한 id 목록은 `metadata`에 JSON 문자열로 내려온다.

- 이름, 아바타 등 사용자 표시 정보는 메시지의 `metadata`만으로 완성하지 않고 팀원 목록 API나 캐릭터 API로 조회해 채운다.
- 전체 `messageType` 목록은 `08-ai.md`의 표를 기준으로 한다.
- REST 메시지 조회는 최신순 최근 50개이므로, 화면 렌더링 시 필요한 정렬 방향을 확인해 UI에 맞게 정렬한다.
- 새 메시지는 `/topic/teams/{teamId}`로 들어오는 이벤트를 기존 메시지 목록에 합쳐 반영한다.

## 4. 팀장 선출 플로우

팀 생성 시 매칭 신청에서 선택한 팀장 희망(`WANTS`) 응답 개수에 따라 서버가 자동으로 모드를 결정한다. 프론트는 `LeaderSelectionMode`를 직접 분기하기보다 채팅에 도착한 카드 종류를 보고 화면을 그린다.

| WANTS 개수 | 모드 | 채팅에 오는 것 |
| --- | --- | --- |
| 1명 | `AUTO_ASSIGNED` | 인사 유도 텍스트 메시지에 팀장 이름이 포함된다. 팀원 전원이 인사를 마치면 `LEADER_RESULT_CARD`가 발행되고 공모전 추천으로 넘어간다. 투표 API 호출은 필요 없다. |
| 2명 이상 | `CANDIDATE_VOTE` | 팀장 여부 투표 없이 사전 희망자 전원을 후보로 담은 `LEADER_VOTE_CARD`가 발행된다. |
| 0명 | `OPEN_NOMINATION` | `LEADER_NOMINATION_CARD`로 팀장 할래요/안할래요 투표부터 시작한다. AI 추천 후보 2명이 함께 내려온다. |

### `LEADER_NOMINATION_CARD`

`OPEN_NOMINATION` 전용 팀장 여부 투표 카드다.

- `metadata.aiRecommendedTeamMemberIds`: AI가 추천한 후보 2명의 `teamMemberId` 배열
- 본인의 `teamMemberId`가 추천 배열에 포함되면 "AI가 당신을 팀장 후보로 추천했어요!" 같은 개인화 문구를 프론트에서 붙여 보여줄 수 있다.
- 사용자가 응답하면 `PATCH /api/teams/{teamId}/leader-candidacy`를 호출한다.

### `LEADER_VOTE_CARD`

팀장 투표와 동률 처리에 재사용되는 카드다. 같은 `messageType`이므로 `metadata` 필드 유무로 구분한다.

| 카드 상황 | metadata | 프론트 처리 |
| --- | --- | --- |
| 최초 투표 카드 | `candidateTeamMemberIds`만 있음 | "팀장 투표" 바텀시트에서 후보를 라디오로 선택한 뒤 `POST /leader-votes` 호출 |
| 동률 카드 | `candidateTeamMemberIds` + `aiRecommendedTeamMemberId` | "재투표하기", "추천 수락하기" 버튼 노출 |
| 재투표 안내 카드 | `candidateTeamMemberIds`만 있음 | 새 카드가 온 뒤 각자 다시 `POST /leader-votes` 호출 |

동률 카드에서 "추천 수락하기"를 누르면 `POST /api/teams/{teamId}/leader-votes/ai-recommendation/accept`를 body 없이 호출한다.

"재투표하기"를 누르면 `POST /api/teams/{teamId}/leader-votes/revote`를 body 없이 호출한다. 이 엔드포인트는 상태를 바꾸지 않고 안내 메시지만 다시 보내는 용도라 여러 명이 동시에 눌러도 안전하지만, 안내 카드가 중복으로 뜰 수 있다. 프론트에서는 버튼을 누른 사람의 화면에서 즉시 버튼을 비활성화하는 정도로 처리한다.

### `LEADER_RESULT_CARD`

팀장 확정 카드이며 모든 경로의 공통 도착점이다.

- `metadata.leaderTeamMemberId`: 최종 팀장의 `teamMemberId`
- 이 카드가 오면 팀장 선출 플로우는 종료된 것이다.
- 이후 서버가 `CONTEST_RECOMMEND_CARD`를 발행해 공모전 추천 단계로 넘어간다.
- 후보가 끝까지 0명이거나 마감까지 아무도 투표하지 않으면 서버가 무작위로 임시 팀장을 지정하고 동일하게 `LEADER_RESULT_CARD`로 안내한다.

## 5. 마감 타이머

팀장 선출 마감 시간은 `GET /api/teams/{teamId}/members` 응답(`TeamMembersResponse`)의 `leaderSelectionDeadlineAt`을 사용한다.

- 서버가 초 단위로 push하는 값이 아니라 고정 시각이다.
- 프론트는 받은 시각과 현재 시각의 차이를 매초 로컬에서 계산해 "투표 마감까지 00:00:00" 카운트다운을 표시한다.
- 마감 이후 자동 확정 같은 처리는 서버 스케줄러가 수행하고, 결과는 평소처럼 채팅 메시지로 도착한다.

## 6. 공모전 추천/후보/투표

공모전 추천과 후보/투표도 팀장 선출과 마찬가지로 대부분 채팅 카드로 진행한다.

- 팀장 확정 후 `CONTEST_RECOMMEND_CARD`가 이어서 도착한다.
- 프론트는 카드 메시지의 `messageType`과 `metadata`를 기준으로 추천, 후보, 투표 UI를 렌더링한다.
- 후보 추가, 후보 삭제, 투표처럼 사용자가 명시적으로 액션하는 부분만 REST API를 호출한다.
- 후보 id 목록이 `metadata`로 내려오면 실제 공모전 이름, 썸네일, 주최 등 표시 정보는 공모전 API로 조회해서 채운다.
- 채팅 추천 공모전 리스트에는 AI가 내려준 후보 2개만 노출한다.
- 사용자가 채팅방에 공유된 공모전 카드의 `+` 버튼을 누르면 안내 팝업이 나오고 해당 공모전을 후보로 추가한다.
- 추천 카드의 `투표하기` 버튼을 누르면 투표 화면에는 AI 후보 2개가 먼저 보이고, 사용자가 추가한 후보가 그 아래에 이어서 보인다.
- 투표 화면 우측 상단의 `+` 버튼은 기존 동작대로 후보를 직접 추가하기 위해 공모전 리스트 화면으로 이동한다.

## 7. 알려진 확인 필요 이슈

`AUTO_ASSIGNED` 케이스에서 Figma는 인사 유도 메시지와 `LEADER_RESULT_CARD`가 동시에, 즉 아무도 인사하기 전에 함께 나타난다.

현재 서버는 인사 유도 텍스트만 먼저 보내고, `LEADER_RESULT_CARD`는 팀원 전원이 인사를 마친 뒤 별도 문구와 함께 발행한다. 의도된 사양 변경인지 프론트/기획과 확인이 필요하다. 확인되면 이 문서와 `02-leader-election.md`를 함께 갱신한다.
